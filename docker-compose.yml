services:
  db:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_USER: erp
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-erp123456}
      POSTGRES_DB: erp
      TZ: Asia/Shanghai
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U erp"]
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      - erp-net

  erp:
    build: .
    restart: always
    ports:
      - "127.0.0.1:8090:8090"
    environment:
      DATABASE_URL: postgres://erp:${POSTGRES_PASSWORD:-erp123456}@db:5432/erp
      SECRET_KEY: ${SECRET_KEY:-change-me-to-a-random-secret-in-production}
      TZ: Asia/Shanghai
    volumes:
      - ./backups:/app/backups
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - erp-net

networks:
  erp-net:
    driver: bridge

volumes:
  pgdata:
